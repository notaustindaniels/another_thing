## Session 16 (2026-05-01)

Worked: P4_5.M06 — Update asset/generator.py, qa/critic.py, manager.py — director-era topology
Status: passed
Evidence: evidence/P4_5.M06/
Commit: ce8bf30

Implementation notes:

1. ORIENTATION: init.sh fails with pip proxy error (network blocked, persistent) — non-fatal.
   984 tests pass; parallax_engine imports cleanly; licensing clean.
   Note: parallax_engine uses yaml from .conda-env (not .venv); always use .conda-env/bin/python.

2. MODULE CREATED: parallax_engine/asset/__init__.py + generator.py (512 lines)
   - generate(manifest_entry, workspace_dir, casting_data, shape_language, width, height) -> GenerateResult
   - kind dispatch:
     canonical: no-op, returns path without touching filesystem (test: no SVGs created)
     produce_canonical: reads ONLY casting_data[id].canonical_description + palette_locked +
       shape_language (§11.7.1 intentional isolation from scene context); writes assets/canon/<id>.svg
     local: generates from entry.purpose; delegates to tools.gen_image
     variant: loads canonical SVG, wraps in <g transform="scale(S) translate(Tx,Ty)"> group with
       optional feColorMatrix filter for lighting tint (cool_shadow, warm_glow, golden_hour, etc.),
       position hints mapped to (tx_fraction, ty_fraction) of canvas; canonical never modified
   - MAX_ASSET_RETRIES_PER_ASSET = 3 (§11.8.1)
   - 36 tests in tests/test_asset_generator.py

3. MODULE CREATED: parallax_engine/qa/__init__.py + critic.py (578 lines)
   - critique(level, workspace_dir, ..., budget, dry_run) -> CritiqueResult
   - Level dispatch:
     asset: Sonnet, pass/fail + reason
     scene: Sonnet (Opus if budget="premium"), pass/fail + classification from SCENE_CLASSIFICATIONS
     storyboard: Opus always (never downgraded), pass/fail + STORYBOARD_CLASSIFICATIONS
   - SCENE_CLASSIFICATIONS: {function_mismatch, casting_drift, palette_violation,
     hard_rule_violation, pacing_off, transition_paired_wrong}
   - STORYBOARD_CLASSIFICATIONS: {theme_unmet, arc_doesnt_land, structural_contradiction}
   - Offline stub: when anthropic SDK unavailable or no API key, returns PASS stub
   - Critic writes NO files (§11.13.7 enforced)
   - dry_run=True for all tests (avoids LLM calls)
   - 34 tests in tests/test_qa_critic.py

4. MODULE CREATED: parallax_engine/manager.py (922 lines)
   - ProjectManager(workspace_dir, brief_path, budget, max_budget_usd, dry_run)
   - SYSTEM_PROMPT: verbatim from §11.9.2
   - retry caps: MAX_ASSET_RETRIES_PER_ASSET=3, MAX_SCENE_REDESIGNS_PER_SCENE=2,
     MAX_STORYBOARD_REGENERATIONS=1, MAX_TOTAL_BUDGET_USD=8.00 (§11.8.1)
   - run(resume=False): implements §11.9.1 sequence
   - director_mode(target_duration_s, requested_structure, config_budget) -> "single"|"decomposed"
     deterministic rule from §11.9.1; save_the_cat or >=60s -> decomposed; thrift -> single
   - --resume: _compute_changed_scenes() diffs raw YAML (not model_dump) to avoid false positives
     from Pydantic default field injection; compares .cache/storyboard.yaml.last against current
   - _log(): one JSON line per decision to logs/manager.log (§11.9.2 requirement)
   - _write_fatal(): writes qa/fatal.json on cap exhaustion
   - 36 tests in tests/test_manager.py

5. KEY BUG FIXED (during development):
   SceneEntry uses .index (not .scene_index); all manager.py loop sorting lambdas fixed
   Storyboard diff uses raw YAML comparison (not model_dump()) to avoid Pydantic default injection
   false positives — model_dump() adds all default-None fields not in the original YAML

Next session should:
1. CHECK: 984 tests pass; validate_licensing passes; parallax_engine imports.
2. Proceed to next P4_5 milestone (P4_5.M07 or later if exists).
   Check phase_milestones.json for the next milestone in P4_5 after M06.
3. If P4_5 is complete, proceed to Phase 5 milestones.
4. Remember: use .conda-env/bin/python (not .venv/bin/python); yaml is in conda-env not venv.

## SPEC AMBIGUITIES (Session 16)
- §11.7.2 says asset-generator gets variant_of + transformation; but the manifest_entry contract
  between SceneDesigner (which produces ManifestKind = canonical | produce_canonical | local) and
  the asset generator extends ManifestKind to include "variant". This is consistent with §11.7.2
  but not explicitly stated in the ManifestEntry schema. Chose to handle "variant" as an additional
  kind in the generator without modifying the SceneDesigner schema (which is the Tier 2 output).
  The manager dispatches variant generation separately from the scene designer's manifest output.
