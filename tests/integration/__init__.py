"""Integration tests for parallax-engine director-era pipeline (P4_5.M07).

These tests require the full package to be installed (editable install
``pip install -e .``) but do NOT require network access or API keys.
All LLM calls are stubbed via ``dry_run=True`` on the ProjectManager.
"""
